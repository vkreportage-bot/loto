/**
 * Normalise les différents formats historiques dans un schéma commun
 * sans perdre les règles propres à chaque période.
 */

async function main() {
  console.log("Normalisation : scaffold prêt.");
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
