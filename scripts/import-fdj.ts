/**
 * Import initial des archives historiques FDJ.
 *
 * À compléter dès que le format exact des fichiers sources
 * et leur mode de récupération sont figés.
 */

async function main() {
  console.log("Import FDJ : scaffold prêt.");
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
